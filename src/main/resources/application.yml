

spring:
  name: multiTenantApp
  datasource:
    url: jdbc:postgresql://localhost:5432/multitenant_db
    username: postgres
    password: 786@admin
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
      
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true
  devtools:
    livereload:
      enabled: true

server:
  port: 8080